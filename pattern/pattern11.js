var area = 0;
var startlimit = 2;
var power = Math.pow(startlimit, 2);
var series1 = 0;
for (let index = 1; index < limit; index++) {

    series1 = series1 + (1 / power);

    console.log(series1);
}
