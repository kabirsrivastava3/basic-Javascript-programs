var limit = 9;

for (var indexRow = 0; indexRow <= n; indexRow++) {
    for (var indexColumn = 0; indexColumn <= indexrow; indexColumn++) {

        process.stdout.write((indexRow * indexColumn).toString());
    }
    console.log("");
}
